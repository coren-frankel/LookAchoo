{
    "version": 2,
    "builds": [
        {
            "src": "./client/build/index.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "/"
        }
    ]
}